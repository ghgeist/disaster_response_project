{
  "name": "Disaster Response",
  "install": "python3 -m pip install --user -U pip virtualenv && python3 -m virtualenv .venv && .venv/bin/python -m pip install -r requirements.txt -r requirements-dev.txt && .venv/bin/python -m pip install -e . && .venv/bin/python -m nltk.downloader stopwords punkt punkt_tab wordnet",
  "terminals": [
    {
      "name": "Flask app",
      "command": ".venv/bin/python run.py"
    }
  ],
  "ports": [5000]
}
