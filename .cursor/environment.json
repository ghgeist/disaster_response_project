{
  "name": "Disaster Response",
  "install": "python3 -m venv .venv && .venv/bin/python -m pip install -U pip && .venv/bin/python -m pip install -r requirements.txt -r requirements-dev.txt && .venv/bin/python -m pip install -e . && .venv/bin/python -m pip install 'scikit-learn==1.7.1' && .venv/bin/python -m nltk.downloader stopwords punkt punkt_tab wordnet",
  "terminals": [
    {
      "name": "Flask app",
      "command": ".venv/bin/python run.py"
    }
  ],
  "ports": [5000]
}
